# 大模型合同类型识别 — 审查报告

**审查日期**：2026-07-01  
**审查范围**：`services/llm_classifier.py`（266行）、`api/contracts.py`、`config.py`、`tests/`

---

## 一、总体结论

**通过**。设计说明书要求的全部功能均已实现，代码质量高，降级策略完善，测试覆盖充分。

---

## 二、逐项审查

### 2.1 `services/llm_classifier.py`

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 合同首部提取 | ✅ | 优先取 `kind=preamble` 节点，无则降级根节点。限制 30 行 + 4000 字符 |
| 类型清单查询 | ✅ | `contract_type.find_all({enabled:1})`，去重，少于 2 个类型时跳过 LLM |
| Prompt 构造 | ✅ | 系统指令 + 类型清单 + 首部正文 + "只返回合同类型名称" |
| LLM 调用 | ✅ | `httpx.post`，temperature=0，timeout 可配置，Authorization header 脱敏日志 |
| 结果校验 | ✅ | 去除引号后检查是否包含清单中某类型。精确匹配→96，前缀→85，包含→70 |
| 降级返回 | ✅ | 任何异常都返回 `None`，调用方静默降级 |
| 日志设施 | ✅ | `llm_classify_log_enabled=true` 时写入 JSONL 日志，含完整 request/response/耗时 |

### 2.2 `api/contracts.py`

| 检查项 | 结果 | 说明 |
|--------|------|------|
| LLM 集成点 | ✅ | 在关键词规则之后、返回响应之前调用 `classify_contract_type` |
| 线程隔离 | ✅ | `asyncio.to_thread()` 避免阻塞事件循环 |
| 异常降级 | ✅ | `except Exception: logger.exception(); llm_result = None` |
| DB 写回 | ✅ | LLM 结果覆盖 `contracts.contract_type` 列 + `updated_at` |
| 降级处理 | ✅ | LLM 失败时使用关键词规则的 `detected_type`（confidence=50） |

### 2.3 `config.py`

| 检查项 | 结果 | 说明 |
|--------|------|------|
| LLM 配置 | ✅ | `llm_api_url`、`llm_api_key`、`llm_model_name` — 已有 |
| 超时配置 | ✅ | `llm_classify_timeout: int = 10` — 新增 |
| 日志开关 | ✅ | `llm_classify_log_enabled: bool = False`、`llm_classify_log_file: Path` — 新增 |

### 2.4 测试

| 检查项 | 结果 |
|--------|------|
| 59 tests passed | ✅ |
| 新增 LLM 分类器测试 | ✅ 7 个 |
| 前端构建 | ✅ 10.68s |

---

## 三、降级策略覆盖

| 场景 | 代码路径 | 行为 |
|------|---------|------|
| `LLM_API_URL` 为空 | `classify_contract_type` line 36-38 | 返回 `None`，日志 `skip/llm_api_url_blank` |
| 首部提取为空 | line 40-43 | 返回 `None`，日志 `skip/preamble_empty` |
| 类型清单 < 2 个 | line 61-63 | 返回 `None`，日志 `skip/type_count_less_than_2` |
| LLM 返回 `None` | line 76-78 | 返回 `None`，日志 `fallback/llm_result_none` |
| 返回值不在清单中 | line 96-107 | 返回 `None`，日志 `fallback/result_not_in_type_names` |
| HTTP/网络错误 | `_call_llm` line 210-220 | 返回 `None`，日志 `llm.error` |
| `contracts.py` 中 LLM 异常 | line 125-127 | `logger.exception`，`llm_result = None` |
| 写回 DB 失败 | line 138-139 | `logger.exception`，不影响上传成功返回 |

8 个降级场景全部覆盖。

---

## 四、亮点

1. **完整的 LLM 调用日志**（`_write_llm_log`）：记录 request/response/耗时/错误，`Authorization` 自动脱敏，由 `llm_classify_log_enabled` 控制开关
2. **首部提取的健壮性**：优先取 preamble 节点，降级根节点内容，限制长度防止 prompt 过长
3. **类型名称去重**（`_enabled_type_names` line 141-142）：防御 DB 脏数据
4. **DB 写回**：LLM 推断结果持久化到 `contracts.contract_type`，合同列表页也能看到准确类型

---

## 五、无问题

代码与设计说明书完全对齐，降级策略周全，测试覆盖充分。可进入联调阶段。
