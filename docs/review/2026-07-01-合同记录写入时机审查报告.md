# 合同记录写入时机调整与文件清理 — 审查报告

**审查日期**：2026-07-01  
**审查范围**：后端 3 文件 + 前端 5 文件

---

## 一、总体结论

**通过**。设计说明书全部要求已实现。62 tests passed，构建通过。

---

## 二、逐项审查

### 2.1 后端

| 检查项 | 结果 | 文件 |
|--------|------|------|
| 上传不再写 contracts | ✅ | `contracts.py` — 仅调用 `extract_contract_meta` 获取类型供 LLM 使用，不写库 |
| `document_tasks` 不再自动写库 | ✅ | `_record_contract` 调用已移除，解析完成只写 `succeeded` 状态 |
| 新 `POST /contracts/audit` | ✅ | 接收 `{fileId, role, points, contractType}`，校验文档存在 → `extract_contract_meta` → `upsert_by_file_id` → `_create_audit_result` |
| 旧 `POST /contracts/{id}/audit` 保留 | ✅ | 复用同一 `_create_audit_result` 函数 |
| `DELETE /files/{id}/artifacts` | ✅ | 删除上传文件 + 解析目录，`shutil.rmtree(ignore_errors=True)` |

### 2.2 前端

| 检查项 | 结果 | 文件 |
|--------|------|------|
| `cleanupUpload(fileId)` | ✅ | `contract.service.js` — `DELETE /files/{id}/artifacts` |
| `startAudit` 改为传 `{fileId, role, points, contractType}` | ✅ | 调用 `POST /contracts/audit` |
| 上传后不设 `contractId` | ✅ | `ReviewWizard.vue` line 65: `reviewStore.contractId = ''` |
| `onStart` 发送 `fileId` + `auditType` | ✅ | line 88-93，接收返回的 `contractId` |
| `backToUpload` 调清理 | ✅ | line 121-125，cleanup + reset + 回到 Step1 |
| `StepSetup` 传递 `auditType` | ✅ | line 141: `emit('start', auditType.value)` |
| `StructureEditor` 返回清理 | ✅ | 先导航回 review，再异步 cleanup（fire-and-forget） |

### 2.3 重构亮点

`_create_audit_result()` 函数提取：

新老两个审核端点共享同一函数，避免代码重复。签名：

```python
def _create_audit_result(repository, contract_id, contract, role, point_ids):
```

### 2.4 验证

| 检查项 | 结果 |
|--------|------|
| pytest | ✅ 62 passed |
| 前端构建 | ✅ 10.63s |

---

## 三、数据流确认

```
上传 → 只保存文件+解析 → 返回 {fileId} → contracts 表无新记录 ✅

返回上传 → cleanupUpload → 删除文件+解析目录 → reset → Step1 ✅
重新上传 → cleanupUpload → 删除文件+解析目录 → reset → Step1 ✅

开始分析 → POST /contracts/audit {fileId, role, points, contractType}
  → 校验文档存在 → extract_contract_meta → upsert_by_file_id ✅
  → _create_audit_result → 返回 {id, status} ✅
```

## 四、无问题

全部改造与设计说明书一致。后端双端点兼容（新旧均可发起审核），前端数据流闭环完整。可进入联调。
